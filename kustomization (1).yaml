apiVersion: v1
kind: ConfigMap
metadata:
  name: oidc-config
  namespace: platform-demo
  labels:
    platform.example.com/contract: identity
data:
  OIDC_ISSUER_URL: https://identity.example.com/realms/enterprise-demo
  OIDC_CLIENT_ID: platform-demo-app
